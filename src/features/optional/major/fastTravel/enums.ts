export enum FastTravelEntityType {
  Trapdoor,
  Crawlspace,
  HeavenDoor,
}

export enum FastTravelState {
  Disabled,
  FadingToBlack,
  ChangingToSameRoom,
  GoingToNewFloor,
  FadingIn,
}

export enum FastTravelEntityState {
  Open,
  Closed,
}
