export enum CustomChargeBarType {
  /**
   * Lead Pencil is the first entry in the enum because it will never disappear and is the most
   * important to see.
   */
  LEAD_PENCIL,

  AZAZELS_RAGE,
  TAINTED_SAMSON,
  BLOODY_LUST,
}
